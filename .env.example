# LLM Configuration (set one)
# Option 1: Browser-Use API (recommended for speed/cost)
BROWSER_USE_API_KEY=your-browser-use-api-key-here

# Option 2: Google AI Studio
GOOGLE_API_KEY=your-google-api-key-here
GEMINI_MODEL=gemini-flash-latest  # Optional, defaults to gemini-flash-latest

# Jumbo.cl Credentials
JUMBO_EMAIL=your-email@example.com
JUMBO_PASSWORD=your-password

# Browser Configuration (optional)
# Set custom browser executable path (e.g., Ungoogled Chromium)
# If not set, auto-detects Ungoogled Chromium at /Applications/Chromium.app/Contents/MacOS/Chromium
# If not found, uses browser-use default
# BROWSER_EXECUTABLE_PATH=/Applications/Chromium.app/Contents/MacOS/Chromium
