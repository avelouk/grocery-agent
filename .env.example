# LLM: set one (Browser-Use preferred if both set)
BROWSER_USE_API_KEY=
GOOGLE_API_KEY=
# Optional when using Google: GEMINI_MODEL=gemini-flash-latest (default; override for a fixed model)

# Jumbo.cl login (if site is logged out)
JUMBO_EMAIL=
JUMBO_PASSWORD=
